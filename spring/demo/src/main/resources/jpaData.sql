-- In case of Jpa we don't need to create the table, Jpa does by itself
--CREATE TABLE jpaPerson(
--    id integer not null,
--    name varchar(255) not null,
--    location varchar(255),
--    birth_date timestamp,
--    primary key(id)
--);

-- maybe inset is not supported in jpa
